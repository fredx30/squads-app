# Scripts executed by /bin/sh (Docker build, CI) must stay LF even on Windows
# checkouts with core.autocrlf=true.
gradlew text eol=lf
*.sh text eol=lf
gradle/wrapper/gradle-wrapper.properties text eol=lf
